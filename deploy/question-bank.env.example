# 复制到 /etc/question-bank.env 并 chmod 600、chown root:deploy
APP_ENV=production
SECRET_KEY=请用 python3 -c "import secrets; print(secrets.token_hex(32))" 生成后粘贴
USE_X_ACCEL=1
# 首次部署创建管理员时临时使用,用后删除本行
# ADMIN_INITIAL_PASSWORD=
