# 内容审计:结论与遗留(2026-07-26/27)

对全库 **358 道**题解做了一轮数学正确性审计:逐题独立验算,每条发现再由两个独立视角
(自己重算 / 尽力驳倒)对抗式复核,只有两个视角都认为成立才动手改。

## 一、做了什么

| 阶段 | 规模 | 结果 |
|---|---|---|
| 逐题验算 | 358/358 道,无遗漏 | 提出 150 条发现 |
| 对抗式复核 | 150 条全部复核 | 确认 139、存疑 11、驳回 0 |
| 出补丁 + 独立验收 | 119 道题 | 验收 114 通过、5 退回重做 |

改动落库两批:**37 处答案级/推导级错误**(28 道题),**102 处轻度问题**(91 道题)。
另修了一处我自己上一轮造成的回归(见下)。

验收拦下的 5 条值得记一笔 —— 它们说明这道关卡不是形式主义:

- **q350**:修订本身引入了**新的**复杂度错误(完全图上 $n$ 次 Bellman–Ford 是 $O(n^4)$
  不是 $O(n^3)$),已改为只提 Floyd–Warshall 并说明 Bellman–Ford 的真实代价。
- **q329**:修订把 $c_2$ 的分母写成 $(x_2-x_1)(x_3-x_2)$ —— 那是 $c_2$ 与 $c_3$ 因子的
  拼接,整体差一个负号,恰好是该警告框本身要防的符号错误。已重写。
- **q100 / q324**:题面转写缺料/写错,但补丁没碰题面。已逐条对照原卷图片订正(见下)。
- **q201**:误报 —— 该题题面字段只是转载声明,正文在日文轨的 `## 問題文` 里,
  是我的材料包把它复制进「题面」小节才显得重复。

## 二、动到题面的三处(都对照过原卷图片)

题面是入试原题的转写,改它就可能变成另一道题。只有当出错的那段本身是**录题者自撰
或漏抄**的内容时才动,且逐条比对原图:

- **q265**:照图整理的边表把 $e\text{–}j=3$ 抄成了 $e\text{–}i=3$。按题面字面跑
  Dijkstra 得到的最短路唯一,与题解给出的三条直接矛盾;按图重算才是三条。
- **q324**:$f_{aba}(bababababa)$ 多抄了一组 `ba`,原卷是 8 字的 $f_{aba}(babababa)$。
  两轨题解与采点原本就以 8 字版为正解,题面与标准答案自相矛盾。
- **q100**:题面只写「用推论规则定义 ND」却没抄规则表。原卷完整排印了 10 条规则,
  其中**无前提公理** $\Gamma\vdash(A\vee(A\supset B))$ 是 (2)(c) 可证的关键 ——
  缺了它,(2)(c) 在纯直觉主义正片段里根本不成立。已按原图补全。

## 三、11 条存疑的裁决(2026-07-27,已全部处置)

逐条独立重算后:**9 条确认为真问题并已改,2 条判定不改**。

| 题号 | 争点 | 裁决 |
|---|---|---|
| q87 | 把 $zf'/f$ 说成可套用本题结论 | **改**。$zf'/f$ 不是椭圆函数(因子 $z$ 破坏周期性),留数和为 $0$ 用不上。Abel 关系式恰恰来自「对边**不**完全相消、余下周期格子点」。已改写成这个说法 |
| q131 | 中文结论框列了 (1)(2)(4) 独缺 (3) | **改**。编号断档看着像漏了;日文轨本来就列了 (3) 的上界 $\frac{\pi R}{R^2-2}\to0$,补进中文结论框 |
| q147 | 采点「典型失点」说连续性是 (2)(3) 的前提 | **改**。本解的 (2)(3) 走差商与余项估计,根本不在积分号下操作。改写成「若改走积分表示这条路才需要」 |
| q235 | 用 $a_n=(-1)^n$ 作「去掉收敛前提就不成立」的反例 | **改**。$(-1)^n$ 的算术平均收敛到 $0$,它反的是**逆命题**。日文轨说法本就正确,中文轨照此改正 |
| q259 | 尖点处左右单侧斜率写作 $\mp 10f(0)$ | **改**。$p(\mu_0)=2F(-5\lvert\mu_0+0.3\rvert)$ 的左导为 $+10f(0)$、右导为 $-10f(0)$,$\mp$ 按「上号在前」读正好相反。改成写死正负号 |
| q273 | $\varphi_*$ 的 $\varphi$ 在中文轨从未定义 | **改**。中文轨确实从未定义,而且与正交系 $\varphi_n$ 同名。改用 $\gamma(x)=(x,\sin\frac1x)$ 并就地给出定义 |
| q280 | 「幾何的直観」把 $P,P'$ 说成在各自平面的「右外侧」 | **改**。实算:$A,B,C,D$ 在平面 $-11x+5y-3z=-40$ 上,而 $P=(10,6,0)$ 给 $-80$($P=2D$,落在平行的另一张平面);像侧 $P'$ 的 $z=2\ne1$。已写明二者都不在各自平面上,「右外侧」只是 $xy$ 方向的定性说法 |
| q311 | 把 $a=-\tfrac12$ 代入声称 $0<a<1$ 的公式 | **改**。公式的成立条件是 $1\pm a>0$(即 $-1<a<1$),(2) 写成 $0<a<1$ 只因题设限定 $a>0$。已写明,两轨同步 |
| q344 | 称 $f$ 为擬距離(pseudometric) | **改**。擬距離要求对称,而 $f$ 的定义里 $A$ 走外层平均、$B$ 走内层。取 $A=\{0\},B=\{0,10\}$ 实算得 $f(A,B)\approx0.69$、$f(B,A)\approx9.31$。已改为「擬似準距離(quasi-pseudometric)」并附反例 |
| q292 | 補題写作 $(E+K)(E-K+K^2)$ | **不改**。原文写的就是 $=E+K^3$,展开正确($-K+K^2+K-K^2+K^3$)。报错方看错了符号 |
| q262 | 設問3(2) 要给出 4 个有序对,只写了部分并标「待补」 | **不改**(也改不了)。采集到的题面图片只有第 1 页,設問3(2) 依赖的図2 不在其中,无从读出父指针。原文已用 `:::warn` 明示待补 —— 这是题源不全,不编造 |

## 四、顺带发现的两件事

- **我上一轮造成的回归,已修**:句読点归一化把 markdown 有序列表的「1. 」也当成句点
  转成了「1。 」,全库 345 处、62 道题。后果是列表不再被渲染成列表。规则已加放行条件,
  数据已修复(`scripts/repair_ordered_lists.py`)。
- **正文字体偏重,已处理(2026-07-27)**:四个阅读字体原本各 1.7–2.4 MB、合计约 8 MB,
  一次全下(巡检里能看到浏览器中途放弃其中一个的请求 `net::ERR_ABORTED`)。
  现按 `unicode-range` 切片:字集按语料词频排序后切成若干片,浏览器只取该页真正用到
  的那几片。子集里的 ~6.7k 汉字兜底集**没有缩小**,只是不再一次全下。UI 字体
  (~125 KB)保持单文件与 preload 不变。

## 五、盘查工具本身的收窄(2026-07-27)

这一轮最花时间的不是改内容,而是让盘查值得读。收窄前:语言盘查 146 处「残留」里
96 处是规则在乱报;完整性盘查 358 道里报了 349 道。**报到九成是假的,真问题就会跟着
被一起忽略** —— 上一轮那三处线上真坏掉的公式,正是这样混在噪声里被判成误报的。

判定边界都写进了测试:`tests/test_audit_language_rules.py`、
`tests/test_solution_completeness_rules.py`、`tests/test_answer_coverage_labels.py`。
每条收窄都有正反两个用例:该报的仍然报,不该报的不再报。
